import Link from 'next/link'

type SearchParams=Record<string,string|string[]|undefined>
function valueOf(v:string|string[]|undefined,f=''){return Array.isArray(v)?v[0]||f:v||f} async function getSearchParams(i:Promise<SearchParams>|SearchParams|undefined){return i?await Promise.resolve(i):{}}
export default async function SaasApiPage({searchParams}:{searchParams?:Promise<SearchParams>|SearchParams}){const p=await getSearchParams(searchParams);const token=valueOf(p.token);const required=process.env.AI_SECRETARY_READ_TOKEN;if(!required||token!==required)return <div className="rounded-2xl border border-yellow-200 bg-yellow-50 p-6 text-yellow-900">閲覧トークンが必要です。</div>;const endpoints=[['POST /api/v1/diagnoses','動画・相談内容からAI診断を生成'],['GET /api/v1/cases/search','症例ライブラリ検索'],['POST /api/v1/proposals','提案書下書き生成'],['GET /api/v1/customers/:id/timeline','顧客タイムライン取得'],['POST /api/v1/parent-consultations','保護者相談の回答候補生成'],['GET /api/v1/quality/reviews','品質管理レビュー取得']];return <div className="space-y-6"><div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-bold text-green-700">Yatabe Method SaaS</p><h2 className="text-2xl font-black text-gray-900">API化ロードマップ</h2><p className="mt-1 text-sm text-gray-500">将来的に他システムからAI診断・症例検索・提案書を利用できる構造にします。</p></div><Link href={`/admin/ai-secretary/saas-os?token=${encodeURIComponent(token)}`} className="rounded-full bg-gray-900 px-4 py-2 text-sm font-bold text-white">OSへ</Link></div><section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><h3 className="text-lg font-black text-gray-900">予定API</h3><div className="mt-4 space-y-3">{endpoints.map(([e,d])=><div key={e} className="rounded-xl bg-gray-50 p-4"><p className="font-mono text-sm font-black text-gray-900">{e}</p><p className="mt-1 text-sm text-gray-600">{d}</p></div>)}</div></section><section className="rounded-2xl border border-yellow-200 bg-yellow-50 p-5 text-yellow-900"><h3 className="font-black">重要</h3><p className="mt-2 text-sm leading-7">現段階では外部公開APIキーは発行しません。テナント分離、監査ログ、レート制限、課金連動、利用規約整備後に公開します。</p></section></div>}
