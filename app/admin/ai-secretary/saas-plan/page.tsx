import Link from 'next/link'

type SearchParams=Record<string,string|string[]|undefined>
function valueOf(v:string|string[]|undefined,f=''){return Array.isArray(v)?v[0]||f:v||f} async function getSearchParams(i:Promise<SearchParams>|SearchParams|undefined){return i?await Promise.resolve(i):{}}
export default async function SaasPlanPage({searchParams}:{searchParams?:Promise<SearchParams>|SearchParams}){const p=await getSearchParams(searchParams);const token=valueOf(p.token);const required=process.env.AI_SECRETARY_READ_TOKEN;if(!required||token!==required)return <div className="rounded-2xl border border-yellow-200 bg-yellow-50 p-6 text-yellow-900">閲覧トークンが必要です。</div>;const rows=[['市場','個人コーチ、スクール、クラブ、海外アカデミー向けの業務OS'],['収益','月額9,800円から198,000円超。導入支援・テンプレ販売・API課金を追加'],['インフラ','Vercel、Supabase、OpenAI/LLM、ストレージ、メール/LINE/カレンダー連携'],['コスト','初期は月1万〜5万円規模。利用増に応じてDB/LLM/APIコストが増加'],['拡張','多言語、SSO、監査ログ、権限管理、専用環境、API公開']];return <div className="space-y-6"><div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-bold text-green-700">Yatabe Method SaaS</p><h2 className="text-2xl font-black text-gray-900">事業計画メモ</h2><p className="mt-1 text-sm text-gray-500">Phase14時点のSaaS化仮説です。市場規模などは最終報告で外部情報も踏まえて補足します。</p></div><Link href={`/admin/ai-secretary/saas-pricing?token=${encodeURIComponent(token)}`} className="rounded-full bg-gray-900 px-4 py-2 text-sm font-bold text-white">料金へ</Link></div><section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div className="space-y-3">{rows.map(([k,v])=><div key={k} className="rounded-xl bg-gray-50 p-4"><p className="text-xs font-black text-green-700">{k}</p><p className="mt-1 text-sm leading-7 text-gray-800">{v}</p></div>)}</div></section></div>}
